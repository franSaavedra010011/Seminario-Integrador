import { GenericRepositoryService } from 'src/shared/utils/genericRepository.service';
import { Repository } from 'typeorm';
import { AbmTurnoEstadoUseCase } from '../abm/turnoEstado/abm-turno-estado.use-case';
import { LocalidadSolicitarTurnoDto } from './dto/localidad-solicitar-turno.dto';
import { Especialidad } from 'src/domain/entities/especialidad.entity';
import { EspecialidadSolicitarTurnoDto } from './dto/especialdiad-solicitar-turno.dto';
import { Hospital } from 'src/domain/entities/hospital.entity';
import { HospitalSolicitarTurnoDto } from './dto/hospital-solicitar-turno.dto';
import { MedicoSolicitarTurnoDto } from './dto/medico-solicitar-turno.dto';
import { AgendasSolicitarTurnoDTO } from './dto/agendas-solicitar-turno.dto';
import { TurnoAgendaDia } from 'src/domain/entities/turno-agenda-dia.entity';
import { Medico } from 'src/domain/entities/medico.entity';
import { Paciente } from 'src/domain/entities/paciente.entity';
import { ResumenSolicitarTurnoDTO } from './dto/resumen-solicitar-turno.dto';
import { Usuario } from 'src/domain/entities/usuario.entity';
import { EstadoTurno } from 'src/domain/entities/estado-turno.entity';
import { AbmTurnoUseCase } from '../abm/turno/abm-turno.use-case';
import { AgendaSemanal } from 'src/domain/entities/agenda-semanal.entity';
export declare class SolicitarTurnoUseCase {
    private readonly genericRepository;
    private especialidadRepository;
    private hospitalRepository;
    private turnoAgendaDiaRepository;
    private medicoDiaRepository;
    private usuarioRepository;
    private estadoTurnoRepository;
    private pacienteRepository;
    private agendaSemanalRepository;
    private readonly abmTurnoEstadoUseCase;
    private readonly abmTurnoUseCase;
    constructor(genericRepository: GenericRepositoryService, especialidadRepository: Repository<Especialidad>, hospitalRepository: Repository<Hospital>, turnoAgendaDiaRepository: Repository<TurnoAgendaDia>, medicoDiaRepository: Repository<Medico>, usuarioRepository: Repository<Usuario>, estadoTurnoRepository: Repository<EstadoTurno>, pacienteRepository: Repository<Paciente>, agendaSemanalRepository: Repository<AgendaSemanal>, abmTurnoEstadoUseCase: AbmTurnoEstadoUseCase, abmTurnoUseCase: AbmTurnoUseCase);
    solicitarTurnoEspecialidades(): Promise<EspecialidadSolicitarTurnoDto[]>;
    solicitarTurnoLocalidades(idEspecialidad: number): Promise<LocalidadSolicitarTurnoDto[]>;
    solicitarTurnoHospitales(idEspecialidad: number, idLocalidad: number): Promise<HospitalSolicitarTurnoDto[]>;
    solicitarTurnoMedicos(idEspecialidad: number, idHospital: number): Promise<MedicoSolicitarTurnoDto[]>;
    solicitarTurnoAgendas(idMedico: number, idHospital: number): Promise<AgendasSolicitarTurnoDTO[]>;
    solicitarTurnoResumen(idMedico: number, idHospital: number, idAgendaSemanal: number, idAgendaDia: number, idTurnoAgendaDia: number, idEspecialidad: number, emailUsuario: string): Promise<ResumenSolicitarTurnoDTO>;
    solicitarTurnoFinalizar(idMedico: number, idHospital: number, idAgendaSemanal: number, idAgendaDia: number, idTurnoAgendaDia: number, idEspecialidad: number, emailUsuario: string): Promise<void>;
    private getWeekNumber;
    private obtenerFechaDesdeDia;
}
